#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    vector<int> A = {5,1,4,2,3};
    sort(A.begin(), A.end()); 

    cout << "Vector ordenado: ";
    for(int x : A) cout << x << " ";
    cout << endl;
}
